Bring in html-common
====================

Bring in html-common submodule and switch over for libs like Maths, HTMLApp etc.

* Split from `task/2.🔎🎑 - visual updates.md`

```
2026-02-28		New task	📽🖧
```


I finally have the initial release for `html-common` tagged - it's not especially complete, but the polar config for planar space *is* passing tests, so that's something.

This task is taking over the current `visual-updates` branch, where I'd already brought in the submodule.
Hopefully there's not much substantive work in here yet.

Just had a quick look through the branch and it's mostly pretty insignificant, so should be good to go:
* Hadn't done much with the svg views yet
* Hadn't started the changes I had in mind for 'proper' light dark themes, except an undercooked attempt at a dark wall-clock
* Some small visual changes that are fine to be included in here
* Have already switched over HTMLApp, Maths and ViewBox


Move submodule, fix to v0.1
---------------------------

I'd put the submodule under the yearclock directory for some reason (my brain is strange sometimes), but now want it in the project root for consistency.
Also will pin year-clock to v0.1 for now.
At these early stages I might still need a few back-and-forth updates, but will see.

### Move submodule
Having not fun trying to move the submodule.
First couple of goes made a complete twisted mess of the local checkout.
(...just managed to revert to working order...)
Git docs look they have options for changing submodule branch and url, but not path afaics.
So try three, will deinit the module entirely and readd it at the new path.

	git submodule deinit "yearclock/[html-common]"


Readd:

	git submodule add "../html-common" [html-common]